# AI-Tracker v2.0

🤖 **Умный Telegram-бот для работы с Yandex Tracker через AI**

AI-Tracker - это интеллектуальный помощник, который использует локальные LLM модели (Ollama) для автоматизации работы с задачами в Yandex Tracker. Бот понимает естественный язык, создает задачи, генерирует дайджесты и управляет проектами через Telegram.

## 🚀 Основные возможности

### 📝 **Создание задач через естественный язык**
- Отправьте описание задачи обычным текстом
- AI автоматически выделит название, описание, очередь, приоритет
- Семантический анализ для выбора подходящей очереди
- Автоматическое определение типа задачи (баг, фича, задача)

### 📊 **Умные дайджесты**
- Автоматические ежедневные отчеты
- Анализ изменений относительно последнего дайджеста
- Группировка задач по статусам через LLM
- Пользовательские расписания

### 🎯 **LLM-классификация статусов**
- Автоматическая классификация статусов задач через AI
- Поддержка любых статусов из Yandex Tracker
- Нормализация в стандартные категории (To Do, In Progress, Done, Blocked)

### ⚙️ **Управление очередями**
- Добавление/удаление очередей для отслеживания
- Просмотр доступных очередей
- Персональные настройки для каждого пользователя

## 🏗️ Архитектура проекта

```
AI-Tracker/
├── app/
│   ├── api/                    # API эндпоинты
│   ├── business_logic/         # Бизнес-логика
│   ├── core/                   # Основные сервисы
│   │   └── digest_service.py   # Генерация дайджестов
│   ├── domain/                 # Доменные модели
│   ├── infrastructure/         # Инфраструктура
│   │   └── database/          # Модели БД
│   ├── prompts/               # Промты для LLM
│   │   └── templates/         # Jinja2 шаблоны
│   ├── scheduler/             # Планировщик дайджестов
│   ├── services/              # Сервисы
│   │   ├── llm/              # LLM провайдеры
│   │   ├── llm_service.py    # Основной LLM сервис
│   │   └── tracker_service.py # Работа с Yandex Tracker
│   └── telegram/              # Telegram бот
│       └── bot.py            # Основной бот
├── docker-compose.yml         # Docker конфигурация
├── requirements.txt           # Python зависимости
└── .env                      # Переменные окружения
```

## 🔧 Технологический стек

- **Backend**: FastAPI, Python 3.11
- **LLM**: Ollama с моделью Vikhr-Gemma-2B-instruct-GGUF
- **База данных**: PostgreSQL
- **Контейнеризация**: Docker & Docker Compose
- **Шаблонизация**: Jinja2 для промтов
- **Планировщик**: APScheduler
- **HTTP клиент**: httpx

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <repository-url>
cd AI-Tracker
cp env.example .env
```

### 2. Настройка переменных окружения
Отредактируйте `.env` файл:
```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Yandex Tracker
YANDEX_TRACKER_TOKEN=your_tracker_token
YANDEX_ORG_ID=your_org_id

# LLM
OLLAMA_MODEL=hf.co/Vikhrmodels/Vikhr-Gemma-2B-instruct-GGUF:Q3_K_L
```

### 3. Запуск
```bash
# Сборка и запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs app
```

### 4. Запуск Telegram-бота
```bash
docker-compose exec app python -c "from app.telegram.bot import TelegramBot; TelegramBot().run_polling()"
```

## 📋 Основные команды бота

### 🔧 Управление очередями
- `/add_queue <ключ>` - Добавить очередь для отслеживания
- `/list_queues` - Показать ваши очереди
- `/remove_queue <ключ>` - Удалить очередь
- `/show_available_queues` - Показать доступные очереди

### 📊 Дайджесты
- `/send_now` - Получить дайджест сейчас
- `/set_schedule HH:MM` - Установить время дайджеста (UTC)

### 🤖 Создание задач
Просто отправьте описание задачи обычным текстом:
```
"Нужно исправить баг в авторизации на главной странице"
"Создать документацию для API платежей"
"Добавить новую функцию экспорта в Excel"
```

## 🧠 Как работает AI

### 1. **Анализ намерений**
Бот использует LLM для понимания, что хочет пользователь:
- Создание задачи
- Получение дайджеста
- Управление очередями
- Общие вопросы

### 2. **Создание задач**
LLM анализирует текст и извлекает:
- **Название** (summary) - краткое описание
- **Описание** (description) - подробные детали
- **Очередь** - семантический выбор из доступных
- **Приоритет** - анализ важности
- **Тип** - баг, фича, задача, эпик
- **Теги** - автоматическое определение

### 3. **Классификация статусов**
LLM нормализует любые статусы из Tracker в стандартные:
- **To Do** - новые, открытые задачи
- **In Progress** - задачи в работе
- **Done** - выполненные задачи
- **Blocked** - заблокированные задачи

### 4. **Генерация дайджестов**
- Анализ изменений с момента последнего дайджеста
- Группировка задач по статусам через LLM
- Создание читаемых отчетов в стиле Agile standup

## 🔄 API эндпоинты

- `GET /health` - Проверка здоровья сервиса
- `GET /model/status` - Статус LLM модели
- `GET /` - Информация о сервисе

## 📁 Структура промтов

Промты используют Jinja2 для динамической генерации:

- `free_conversation.md` - Анализ намерений пользователя
- `create_task.md` - Создание структурированных задач
- `status_classification.md` - Классификация статусов
- `changes_summary.md` - Резюме изменений
- `queue_summary.md` - Дайджест очереди

## 🔧 Конфигурация

### Таймауты
- LLM запросы: 300 секунд (для стабильной работы)
- HTTP клиент: 300 секунд
- Планировщик: ежедневно в 9:00 UTC

### Модели
- **Основная**: `hf.co/Vikhrmodels/Vikhr-Gemma-2B-instruct-GGUF:Q3_K_L`
- **Fallback**: `deepseek-r1:1.5b`

## 🐛 Устранение неполадок

### LLM не отвечает
```bash
# Проверить статус Ollama
docker-compose exec ollama ollama list

# Проверить логи
docker-compose logs app | grep -i "ollama"
```

### Бот не отвечает
```bash
# Перезапустить бота
docker-compose restart app

# Проверить токен в .env
grep TELEGRAM_BOT_TOKEN .env
```

### Проблемы с БД
```bash
# Проверить подключение
docker-compose exec postgres psql -U tracker_user -d tracker_db
```

## 📈 Мониторинг

- **Логи приложения**: `docker-compose logs app`
- **Логи Ollama**: `docker-compose logs ollama`
- **Статус сервисов**: `docker-compose ps`
- **Health check**: `curl http://localhost:8000/health`

## 🤝 Разработка

### Добавление новой LLM модели
1. Загрузите модель в Ollama
2. Обновите `OLLAMA_MODEL` в `.env`
3. Перезапустите сервисы

### Создание нового промта
1. Добавьте `.md` файл в `app/prompts/templates/`
2. Используйте Jinja2 для переменных
3. Обновите `PromptLoader` при необходимости

### Расширение функционала
- Добавьте новый обработчик в `TelegramBot`
- Создайте соответствующий промт
- Обновите `LLMService` для новой логики

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

---

**AI-Tracker v2.0** - Умный помощник для работы с Yandex Tracker через AI 🤖 